name: 99 Check Admin TOTP (임시)

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Install psql
        run: sudo apt-get install -y postgresql-client

      - name: Show admin TOTP secret
        env:
          PGPASSWORD: "Sepang2026!"
        run: |
          psql "postgresql://postgres.apghgbecayjfsuswaggf@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres" \
            -c "SELECT email, totp_secret, is_active FROM users WHERE role='ADMIN';"
